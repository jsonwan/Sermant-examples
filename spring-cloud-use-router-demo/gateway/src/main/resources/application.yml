spring:
  application:
    name: gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: false
          lower-case-service-id: true
      routes:
        - id: backend
          uri: lb://backend
          predicates:
            - Path= /backend/**
          filters:
            - StripPrefix=1
server:
  port: 8080
